# Core dependencies for OctoTools framework
# Build system dependencies (required for Python 3.13)
# Install these first to ensure build tools are available
setuptools>=65.0.0
wheel>=0.40.0
build>=0.10.0
cython>=3.0.0  # Required for building scikit-learn on Python 3.13
numpy>=1.21.0,<2.0  # Install numpy early as many packages depend on it

python-bidi==0.4.2
easyocr==1.7.1
openai==1.58.1
python-dotenv==1.0.1
wikipedia==1.4.0
pillow==10.4.0
platformdirs==4.2.2
sympy>=1.13.3
tenacity==9.0.0
diskcache==5.6.3
# Updated transformers and tokenizers for Python 3.13 compatibility
# tokenizers will be installed automatically as a dependency of transformers
transformers>=4.44.2  # Updated to allow newer versions with Python 3.13 support
pymed==0.8.9
metapub==0.5.12
gradio==5.23.1

# Deep learning and GPU support
torch>=2.0.0
torchvision>=0.15.0
torchmetrics>=0.11.0  # Required for DINOv3 model evaluation

# DINOv3 dependencies (required when loading model from GitHub)
ftfy>=6.0.0  # Text processing for dino.txt tokenizer
omegaconf>=2.3.0  # Configuration management
regex>=2022.0.0  # Text processing for dino.txt tokenizer
submitit>=1.4.0  # Distributed training support
termcolor>=2.0.0  # Colored terminal output for logging

# Scientific computing and data analysis
# Updated versions for Python 3.13 compatibility
# Note: numpy is installed above in build dependencies section
pandas>=2.1.0,<3.0  # Updated for Python 3.13 support (pandas 2.1+ supports Python 3.13)
scikit-learn>=1.4.0,<2.0  # Updated for Python 3.13 support (scikit-learn 1.4+ supports Python 3.13)
scipy>=1.7.0

# Image processing and computer vision
opencv-python>=4.8.0
scikit-image>=0.19.0
tifffile>=2021.0.0

# Single-cell analysis and bioinformatics
anndata>=0.8.0
scanpy>=1.9.0
umap-learn>=0.5.0

# Cell segmentation and analysis
cellpose>=2.2.0

# Visualization
matplotlib>=3.5.0,<4.0  # Updated for Python 3.13 support (matplotlib 3.10+ supports Python 3.13)
seaborn>=0.12.0

# Utilities and dependencies
huggingface-hub>=0.19.0
requests>=2.25.0
packaging>=21.0
joblib>=1.1.0
tqdm>=4.65.0

# Additional bioinformatics tools (required for Leiden clustering)
leidenalg>=0.9.0  # For Leiden clustering (required by scanpy.tl.leiden)
python-igraph>=0.9.0  # For network analysis (required by leidenalg)